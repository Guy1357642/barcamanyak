# פריסה עצמאית ל-Cloudflare — בלי OpenAI Sites

המדריך הזה מעלה את BarcaManyak לחשבון Cloudflare שלך. כל הפקודות באנגלית להעתקה, ההסברים בעברית.

**מה שצריך:** מחשב (לא טלפון — אין דרך להריץ את זה מהנייד), Node.js 22.13 ומעלה, וחשבון Cloudflare חינמי.
**זמן:** בערך 40 דקות בפעם הראשונה.

בדקתי את השלבים האלה על הפרויקט הזה עד לנקודה שדורשת את החשבון שלך: `wrangler deploy --dry-run` עבר,
ארז 37 מודולים, קרא 128 קבצי assets, וזיהה את שני ה-bindings. מה שלא יכולתי לבדוק זה השלבים שדורשים
התחברות לחשבון — שם אתה עלול להיתקל בהבדלים.

---

## 1. הכנת המחשב

```bash
node --version          # צריך 22.13 ומעלה
npm install -g pnpm
```

פרוס את ה-ZIP לתיקייה, ואז:

```bash
cd BarcaManyak
pnpm install
```

## 2. התחברות ל-Cloudflare

```bash
npx wrangler login
```

זה פותח דפדפן. אם אין לך חשבון, תיצור שם אחד — התוכנית החינמית כוללת Workers ו-D1 ומספיקה לאפליקציה הזאת.

## 3. יצירת מסד הנתונים

```bash
npx wrangler d1 create barcamanyak
```

הפקודה מדפיסה `database_id` — מזהה בפורמט uuid. **שמור אותו**, צריך אותו בשלב הבא.

אין צורך ליצור טבלאות. האפליקציה יוצרת אותן לבד בפנייה הראשונה (`CREATE TABLE IF NOT EXISTS`).

## 4. בנייה ופריסה

```bash
pnpm build
D1_DATABASE_ID=<המזהה-מהשלב-הקודם> node scripts/prepare-cloudflare.mjs
npx wrangler deploy -c dist/server/wrangler.json
```

הסקריפט באמצע מחליף את מזהה מסד הנתונים המדומה שהבנייה מייצרת במזהה האמיתי שלך, ומגדיר את
binding ה-ASSETS. בסוף הפריסה תקבל כתובת בסגנון `https://barcamanyak.<something>.workers.dev` —
זו האפליקציה החדשה שלך.

## 5. הכנסת מפתח ה-API

בלי השלב הזה האפליקציה עובדת מצוין עם לוח המשחקים המקומי, אבל "רענון תוצאה", התצוגה החיה
ותוצאות הטבלה שמתעדכנות בזמן אמת לא יעבדו — הן צריכות מפתח אמיתי מול football-data.org
(הוא מכסה את הליגה הספרדית וסופרקופה בחינם; ליגת האלופות מגיעה מ-TheSportsDB בלי מפתח בכלל).

אם אין לך מפתח עדיין — נרשמים בחינם ב-https://www.football-data.org/client/register ומקבלים
אותו במייל תוך כמה דקות.

```bash
npx wrangler secret put FOOTBALL_DATA_KEY
```

הפקודה תבקש להדביק את המפתח. הוא נשמר מוצפן אצל Cloudflare ולא נכנס לקוד ולא ל-Git.
**אל תשים את המפתח בשום קובץ בפרויקט.**

(שים לב: השם חייב להיות בדיוק `FOOTBALL_DATA_KEY` — זה השם שהקוד קורא בפועל. אם כבר הגדרת בעבר
secret בשם `API_FOOTBALL_KEY` לפי גרסה ישנה יותר של המדריך הזה, הוא לא נקרא על ידי הקוד ואפשר
למחוק אותו עם `npx wrangler secret delete API_FOOTBALL_KEY`.)

## 6. העברת הנתונים מהאתר הקיים

זה מעביר את הפרופילים, ההימורים, התוצאות, הסמלים שהעלית והמשחקים. הוא רק קורא מהאתר הישן ולא מוחק ממנו כלום.

קודם הצצה בלי לכתוב, כדי לוודא שהוא מוצא את הנתונים:

```bash
node scripts/migrate-data.mjs \
  --from https://barcamanyak-friends.empire-media-2222.chatgpt.site \
  --dry-run --backup league-backup.json
```

זה גם שומר גיבוי מקומי. אם הרשימה נראית נכונה, העבר בפועל:

```bash
node scripts/migrate-data.mjs \
  --from https://barcamanyak-friends.empire-media-2222.chatgpt.site \
  --to   https://barcamanyak.<your-subdomain>.workers.dev
```

## 7. אחרי הפריסה

- פתח את הכתובת החדשה, בדוק שהחברים מופיעים ושההימורים במקום.
- הוסף אותה למסך הבית בטלפון, ושלח לחברים את הכתובת החדשה.
- **האתר הישן ממשיך לעבוד** על הגרסה הקודמת. אל תמחק אותו עד שאתה בטוח שהחדש תקין.
  שים לב שמאותו רגע יש שני עולמות נפרדים — הימור באתר הישן לא יופיע בחדש.

## עדכונים בעתיד

```bash
pnpm install
pnpm test
pnpm build
D1_DATABASE_ID=<המזהה> node scripts/prepare-cloudflare.mjs
npx wrangler deploy -c dist/server/wrangler.json
```

מסד הנתונים נשאר — פריסה לא מוחקת נתונים.

---

## דברים שכדאי לדעת

**ה-API של האחסון פתוח לגמרי.** כל מי שיודע את הכתובת יכול לקרוא ולכתוב את כל נתוני הליגה
דרך `/api/storage`, בלי סיסמה. בין שישה חברים זה בסדר, אבל אל תפרסם את הכתובת ואל תשמור שם
שום דבר רגיש. אם תרצה, אפשר להוסיף בהמשך סוד משותף לכתיבה.

**המכסה החינמית של API-Football היא 100 בקשות ליום** והאפליקציה עוצרת לבד ב-92.
זה נמדד לפי החשבון, לא לפי האתר — כלומר אם גם האתר הישן וגם החדש רצים עם אותו מפתח,
הם חולקים את אותה מכסה. אחרי שהחדש עובד, כדאי להפסיק להשתמש בישן.

**Cloudflare Images** לא מוגדר, וזה בכוונה. ה-worker משתמש בו רק בנתיב `/_vinext/image`
שהאפליקציה הזאת לא קוראת לו בכלל. אם בעתיד תוסיף רכיב תמונה של vinext, תצטרך להוסיף
binding בשם `IMAGES`.

**`app/chatgpt-auth.ts` הוא קוד מת** — שום דבר לא מייבא אותו. אפשר להשאיר או למחוק, זה לא משנה.
